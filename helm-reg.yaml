
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "photoroster-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis-session,reg,redis-cache"

serviceAccountName: photoroster-reg

cluster: es-test