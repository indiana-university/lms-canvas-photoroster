
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

ingress:
  enabled: true
  hosts:
    - host: "photoroster-snd.apps-snd.iu.edu"
      paths: []

activeProfiles: "redis,snd,redis-cache"

serviceAccountName: photoroster-snd

cluster: es-test