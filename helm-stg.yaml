
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-stg

env:
  APP_ENV: stg

ingress:
  enabled: true
  hosts:
    - host: "photoroster-stg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis-session,stg,redis-cache"

serviceAccountName: photoroster-stg

cluster: es-test